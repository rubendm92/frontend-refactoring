{
  "name": "frontend-refactoring",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "$(npm bin)/serve -p 8000",
    "test": "$(npm bin)/mocha --require babel-polyfill --require babel-register tests/unit && $(npm bin)/mocha --require babel-polyfill --require babel-register tests/integration && $(npm bin)/mocha tests/e2e"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-register": "^6.26.0",
    "chai": "^4.1.2",
    "mocha": "^5.0.5",
    "puppeteer": "^1.2.0",
    "serve": "^6.5.3",
    "sinon": "^4.5.0"
  }
}
