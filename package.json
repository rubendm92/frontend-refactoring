{
  "name": "frontend-refactoring",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "$(npm bin)/serve -p 8000",
    "test": "$(npm bin)/mocha tests/unit && $(npm bin)/mocha tests/integration && $(npm bin)/mocha tests/e2e"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.0.5",
    "puppeteer": "^1.2.0",
    "serve": "^6.5.3",
    "sinon": "^4.5.0"
  }
}
